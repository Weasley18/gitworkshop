# Orphan rewrite (workshop)

This tree was created with `git switch --orphan`. It shares **no commit history** with `main`. Merging this branch into `main` without a special flag is refused by Git (`unrelated histories`).

## Run

```bash
python3 alt/main.py
```

This is intentionally different from the Node app on `main`.
